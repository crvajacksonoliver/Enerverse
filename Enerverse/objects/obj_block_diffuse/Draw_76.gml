if (!surface_exists(s0))
	s0 = surface_create(room_width, room_height);
if (!surface_exists(s1))
	s1 = surface_create(room_width, room_height);
if (!surface_exists(s2))
	s2 = surface_create(room_width, room_height);